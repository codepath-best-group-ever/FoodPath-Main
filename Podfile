# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'FoodPath' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FoodPath

  target 'FoodPathTests' do
    inherit! :search_paths
    # Pods for testing
      pod 'Parse'
     #pod 'AlamoFireImage'
  end

  target 'FoodPathUITests' do
    # Pods for testing
  end

end
